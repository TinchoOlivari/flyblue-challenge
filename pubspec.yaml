name: flyblue_test
description: Pantalla con detalles de un resultado de un vuelo charter.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  video_player: ^1.0.1
  cached_network_image: ^2.5.1
  cupertino_icons: ^1.0.0
  carousel_slider: ^3.0.0
  numberpicker: ^1.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/
